#!/usr/bin/env bash
# Local preview. Binds 0.0.0.0 so the port forward out of the container works.
set -euo pipefail
exec bundle exec jekyll serve --host 0.0.0.0 --port 4000 --livereload --drafts --incremental "$@"
